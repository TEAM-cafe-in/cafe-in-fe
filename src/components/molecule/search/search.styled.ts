import { Box, styled } from '@mui/material';

export const StyledBox = styled(Box)(() => ({
  display: 'flex',
  alignItems: 'center',
  backgroundColor: '#F8F8F8',
  margin: '15px 15px 18px 30px',
  borderRadius: '4px',
}));
